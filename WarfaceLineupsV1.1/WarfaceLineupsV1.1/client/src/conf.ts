export namespace Config {
    export const isDevelopment: boolean = true;
    export const screenResolution: {width: number, height: number} = {
        width: window.screen.width,
        height: window.screen.height
    }
}